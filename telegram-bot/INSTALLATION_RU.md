# 🤖 Установка Telegram Бота для RobBob Launcher

Подробная инструкция по установке и настройке Telegram бота для верификации пользователей.

---

## 📋 Что потребуется

1. Аккаунт Telegram
2. Аккаунт на Cloudflare (бесплатный)
3. 10-15 минут времени

---

## 🚀 Шаг 1: Создание Telegram бота

### 1.1. Откройте BotFather
- Перейдите к [@BotFather](https://t.me/BotFather) в Telegram
- Нажмите **Start** или отправьте `/start`

### 1.2. Создайте нового бота
Отправьте команду:
```
/newbot
```

### 1.3. Задайте имя бота
BotFather попросит ввести **отображаемое имя** бота (можно изменить позже):
```
RobBob Verification Bot
```

### 1.4. Задайте username бота
Придумайте **уникальный username** (должен заканчиваться на `bot`):
```
robbob_verify_bot
```

### 1.5. Сохраните токен
BotFather отправит вам токен в формате:
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789
```

**⚠️ ВАЖНО:** Сохраните этот токен! Он понадобится для настройки бота.

---

## 📢 Шаг 2: Создание Telegram канала

### 2.1. Создайте новый канал
1. В Telegram откройте меню → **Новый канал**
2. Введите название: `RobBob Updates` (или другое)
3. Введите описание канала
4. Выберите **Публичный канал**

### 2.2. Задайте username канала
Придумайте username канала:
```
robbob_channel
```

### 2.3. Добавьте бота в канал
1. Зайдите в настройки канала
2. Нажмите **Администраторы** → **Добавить администратора**
3. Найдите вашего бота (по username из шага 1.4)
4. **Включите права:**
   - ✅ Просмотр сообщений (обязательно!)
   - Остальные права можно оставить выключенными

### 2.4. Получите ID канала (опционально)
Если нужен числовой ID вместо @username:
1. Перешлите любое сообщение из канала боту [@userinfobot](https://t.me/userinfobot)
2. Он покажет ID канала в формате `-1001234567890`

---

## ☁️ Шаг 3: Развертывание на Cloudflare Workers

### 3.1. Зарегистрируйтесь на Cloudflare
1. Перейдите на [Cloudflare](https://dash.cloudflare.com/sign-up)
2. Создайте бесплатный аккаунт
3. Подтвердите email

### 3.2. Перейдите в Workers
1. Войдите в [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. В левом меню выберите **Workers & Pages**
3. Нажмите **Create application**
4. Выберите **Create Worker**

### 3.3. Настройте Worker
1. Придумайте имя (например: `robbob-bot`)
2. Нажмите **Deploy** (пока с дефолтным кодом)
3. После создания нажмите **Edit code**

### 3.4. Замените код
1. Удалите весь код в редакторе
2. Откройте файл `telegram-bot/worker.js` из репозитория
3. Скопируйте весь код и вставьте в редактор Cloudflare

### 3.5. Настройте переменные
В начале кода найдите эти строки и замените значения:

```javascript
const BOT_TOKEN = 'YOUR_BOT_TOKEN_HERE';  // ← Вставьте токен из шага 1.5
const CHANNEL_ID = '@robbob_channel';      // ← Вставьте @username канала из шага 2.2
const SECRET_KEY = 'your-secret-key-for-codes';  // ← Придумайте любую секретную строку
```

**Пример:**
```javascript
const BOT_TOKEN = '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789';
const CHANNEL_ID = '@robbob_channel';
const SECRET_KEY = 'my_super_secret_key_12345';
```

### 3.6. Сохраните и задеплойте
1. Нажмите **Save and Deploy**
2. Скопируйте URL вашего Worker (например: `robbob-bot.your-subdomain.workers.dev`)

---

## 🔗 Шаг 4: Установка Webhook

### 4.1. Откройте специальный URL
Скопируйте этот URL и замените значения:

```
https://api.telegram.org/bot<BOT_TOKEN>/setWebhook?url=https://<WORKER_NAME>.workers.dev/webhook
```

**Замените:**
- `<BOT_TOKEN>` - токен бота из шага 1.5
- `<WORKER_NAME>` - имя вашего Worker из шага 3.6

**Пример готового URL:**
```
https://api.telegram.org/bot1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789/setWebhook?url=https://robbob-bot.your-subdomain.workers.dev/webhook
```

### 4.2. Откройте URL в браузере
Вставьте готовый URL в адресную строку браузера и нажмите Enter.

### 4.3. Проверьте результат
Вы должны увидеть ответ:
```json
{
  "ok": true,
  "result": true,
  "description": "Webhook was set"
}
```

Если видите `"ok": true` - webhook установлен успешно! ✅

---

## ⚙️ Шаг 5: Настройка лаунчера

### 5.1. Откройте config.js
Откройте файл `lib/config.js` в проекте лаунчера.

### 5.2. Обновите конфигурацию
Найдите секцию `telegram:` и замените значения:

```javascript
telegram: {
  botUsername: 'robbob_verify_bot',           // ← Username бота из шага 1.4
  channelUsername: 'robbob_channel',          // ← Username канала из шага 2.2
  channelId: '@robbob_channel',               // ← @username или числовой ID
  apiUrl: 'https://robbob-bot.your-subdomain.workers.dev',  // ← URL Worker из шага 3.6
  botLink: 'https://t.me/robbob_verify_bot',  // ← Ссылка на бота
  channelLink: 'https://t.me/robbob_channel'  // ← Ссылка на канал
}
```

### 5.3. Сохраните файл
Сохраните изменения в `lib/config.js`.

---

## ✅ Шаг 6: Тестирование

### 6.1. Проверьте бота
1. Напишите боту в Telegram команду `/start`
2. Бот должен ответить приветственным сообщением
3. Если бот молчит - проверьте webhook из шага 4

### 6.2. Проверьте верификацию
1. Подпишитесь на канал
2. Напишите боту `/verify`
3. Бот должен выдать код верификации

### 6.3. Проверьте лаунчер
1. Запустите лаунчер
2. Должно появиться модальное окно с требованием верификации
3. Введите код от бота
4. Верификация должна пройти успешно

---

## 🐛 Решение проблем

### Бот не отвечает
- ✅ Проверьте что webhook установлен (шаг 4)
- ✅ Проверьте что BOT_TOKEN правильный
- ✅ Убедитесь что Worker задеплоен

### Бот говорит "не подписаны на канал"
- ✅ Проверьте что вы подписались на канал
- ✅ Проверьте что бот добавлен администратором канала
- ✅ Проверьте что у бота есть права на просмотр участников
- ✅ Проверьте правильность CHANNEL_ID в worker.js

### Код не принимается в лаунчере
- ✅ Проверьте что apiUrl в config.js правильный
- ✅ Проверьте что Worker доступен (откройте URL Worker в браузере)
- ✅ Проверьте что код не истек (действителен 10 минут)

### Worker не работает
- ✅ Проверьте логи в Cloudflare Dashboard
- ✅ Убедитесь что все переменные заполнены правильно
- ✅ Проверьте что нет синтаксических ошибок в коде

---

## 📊 Команды бота

После настройки бот будет поддерживать следующие команды:

- `/start` - Приветствие и инструкции
- `/verify` - Получить код верификации (требует подписку на канал)
- `/status` - Проверить статус подписки
- `/help` - Справка по использованию

---

## 🔒 Безопасность

- **Не публикуйте** BOT_TOKEN нигде
- **Не публикуйте** SECRET_KEY
- Храните токены только на сервере
- Коды верификации действуют только 10-20 минут
- Все запросы защищены CORS

---

## 💡 Дополнительные возможности

### Логирование
Cloudflare Workers предоставляет логи в реальном времени:
1. Откройте ваш Worker в Dashboard
2. Перейдите на вкладку **Logs**
3. Включите **Real-time logs**

### Изменение времени действия кода
В `worker.js` найдите функцию `generateVerificationCode`:
```javascript
const timeSlot = Math.floor(timestamp / 600);  // 600 секунд = 10 минут
```
Измените `600` на нужное количество секунд.

### Добавление дополнительных команд
Добавьте обработку в функцию `handleTelegramWebhook`:
```javascript
if (text === '/mycommand') {
  await sendMessage(chatId, 'Ваш ответ');
  return new Response('OK');
}
```

---

## 🎉 Готово!

Теперь ваш Telegram бот настроен и готов к использованию!

**Что делает система:**
1. ✅ Пользователь запускает лаунчер → видит модальное окно
2. ✅ Пользователь подписывается на канал
3. ✅ Пользователь получает код от бота
4. ✅ Пользователь вводит код в лаунчер
5. ✅ Система проверяет подписку каждые 5 минут
6. ✅ Если пользователь выходит из канала → доступ автоматически отзывается

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте все шаги заново
2. Посмотрите логи в Cloudflare Dashboard
3. Проверьте что бот отвечает на команды в Telegram

Удачи! 🚀
